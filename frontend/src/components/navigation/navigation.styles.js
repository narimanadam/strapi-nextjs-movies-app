import styled from "@emotion/styled";
import tw from "twin.macro";

export const Navigation = styled.nav(tw``);

export const List = styled.ul(tw`
    flex
    items-center
`);

export const Item = styled.li(tw`
    mr-2
    text-white
`);
