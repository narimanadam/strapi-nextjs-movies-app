import React from "react";
import * as Styled from "./page-title.styles";

export const PageTitle = ({ title }) => {
  return <Styled.PageTitle>{title}</Styled.PageTitle>;
};

export default PageTitle;
