import styled from "@emotion/styled";
import tw from "twin.macro";

export const MovieItem = styled.div(tw`
    container
    mx-auto
    mt-16
`);

export const MovieDetailsWrapper = styled.div(tw`
    flex

`);

export const Poster = styled.img(tw`
    max-w-sm
`);

export const Wrapper = styled.div(tw`
    pl-8
`);

export const Title = styled.h3(tw`
    text-white
`);

export const Date = styled.h3(tw`
    text-white

`);

export const Body = styled.span(tw`
    text-white
`);
